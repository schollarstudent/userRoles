extends ../layout

block content
    div.content
        h1 Your role is #{user.role.name}

        h3 You can
        ul
            each permission in user.role.permission
                li=permission.name